{
  "name": "@agent-nekko/cloud",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Kotrain Cloud, multi-account hosted edition. Wraps the same host engine + UI per account behind accounts/auth/entitlements.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/static": "^10.1.2",
    "@fastify/websocket": "^11.3.0",
    "@agent-nekko/host": "*",
    "@agent-nekko/shared": "*",
    "fastify": "^5.11.2"
  }
}
