node_modules/
dist/
out/
build/
# but keep the desktop app's icon resources (electron-builder buildResources)
!apps/desktop/build/
!apps/desktop/build/**
*.log
.DS_Store
.env
.env.local
coverage/
*.tsbuildinfo
release/
cli-dist/
# runtime artifacts written by training/goal/design runs when this repo is
# itself used as an Agent Nekko workspace (dogfooding).
nekko-training/
nekko-goal/
nekko-designs/

# Local agent tooling (per-machine hooks and critique scratch)
.codex/
.impeccable/

# Agent worktrees checked out inside the repo (scratch, never source)
.worktrees/
